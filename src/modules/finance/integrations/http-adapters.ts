/**
 * Future microservice implementations of finance's integration contracts.
 *
 * When `project` / `hr` are extracted into standalone services, implement the
 * `ProjectLookupAdapter` / `EmployeeLookupAdapter` ports from `contracts.ts`
 * against their HTTP APIs here, and swap the wiring in `api.ts` from
 * `local-adapters` to these. The rest of finance depends only on the contract
 * interfaces, so no service/capability/workflow code changes.
 *
 * Intentionally empty until the first module is extracted.
 */
export {};
