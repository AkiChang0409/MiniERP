/**
 * Future microservice implementations of project's integration contracts.
 *
 * When `sales-crm` / `hr` / `finance` are extracted into standalone services,
 * implement the `CustomerLookupAdapter` / `PeopleLookupAdapter` /
 * `FinanceSummaryAdapter` ports from `contracts.ts` against their HTTP APIs
 * here, and swap the wiring from `local-adapters` to these. The rest of project
 * depends only on the contract interfaces, so no service/route code changes.
 *
 * Intentionally empty until the first module is extracted.
 */
export {};
